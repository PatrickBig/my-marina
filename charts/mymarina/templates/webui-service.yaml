apiVersion: v1
kind: Service
metadata:
  name: {{ include "mymarina.webui.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mymarina.labels" . | nindent 4 }}
    app.kubernetes.io/component: webui
spec:
  type: ClusterIP
  selector:
    {{- include "mymarina.webui.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.webui.port }}
      targetPort: http
      protocol: TCP
      name: http
