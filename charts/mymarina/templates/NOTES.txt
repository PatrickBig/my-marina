MyMarina has been deployed to namespace {{ .Release.Namespace }}.

Application URLs:
  https://{{ .Values.ingress.appHost }} — React SPA
  https://{{ .Values.ingress.apiHost }} — .NET API

All secrets are managed automatically:
  - Database credentials: provisioned by the Crunchy Postgres Operator
  - Redis credentials:    generated by Helm on first install ({{ include "mymarina.redis.credentialSecret" . }})
  - JWT signing key:      generated by Helm on first install ({{ include "mymarina.jwt.credentialSecret" . }})

To deploy from scratch:

  helm install mymarina ./charts/mymarina \
    --namespace mymarina-prod \
    --create-namespace
