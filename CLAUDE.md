# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This repository is currently empty. Update this file once the project is initialized with:

- **Build / lint / test commands** — how to build the project, run the linter, and execute tests (including how to run a single test file or test case).
- **Architecture overview** — the high-level structure: main directories, key entry points, how the layers interact, and any non-obvious design decisions.
- **Tech stack** — frameworks, runtimes, databases, and external services in use.
